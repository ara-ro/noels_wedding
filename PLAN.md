# 모바일 청첩장 개발 계획서

## 1. 프로젝트 개요

- **목적**: 결혼식 모바일 청첩장 제작 및 GitHub 기반 자동 배포
- **형태**: 세로 스크롤 1페이지(SPA) 모바일 웹. 카카오톡/문자 링크 공유가 주 배포 경로.
- **구조**: 프론트엔드(정적, GitHub Pages)는 그대로 유지하되, **하객 사진 업로드 기능**을 위해 백엔드(BaaS)를 추가한 하이브리드 구조.
- **핵심 요구사항**:
  1. 청첩장을 받는 대상(보내는 이 기준)에 따라 **폰트 크기 / 계좌번호 노출 순서 / 이름 표기 순서·문구**가 달라져야 함.
  2. 하객들이 결혼식 당일 찍은 사진을 **직접 업로드**할 수 있고, 업로드된 사진이 갤러리 형태로 모여야 함 (백엔드 필요).

---

## 2. 기술 스택

| 영역 | 선택 | 이유 |
|---|---|---|
| 프레임워크 | **Vite + React + TypeScript** | 백엔드 불필요한 정적 SPA. Next.js의 SSR/서버 기능은 이 프로젝트엔 과함. Vite는 빌드 빠르고 정적 산출물(dist)만 나와서 GitHub Pages 배포에 최적. TS로 "발신자별 분기 데이터" 타입 안정성 확보. |
| 스타일 | **Tailwind CSS** | 모바일 반응형 유틸리티, 폰트 크기 스케일(발신자별 분기)을 클래스 조합으로 빠르게 처리 가능 |
| 라우팅/분기 | React Router 불필요, **URL 쿼리 파라미터**(`?to=`)로 분기 | 페이지 이동이 없는 1페이지 구조라 라우터는 오버엔지니어링. 쿼리 파라미터가 카톡 공유 링크 분기에 가장 단순함 |
| 지도 | 카카오맵 SDK 또는 네이버지도 SDK (iframe 임베드) | 오시는 길 섹션 |
| 공유 | 카카오톡 공유하기 SDK (Kakao JS SDK) | 국내 청첩장 표준 공유 방식 |
| **백엔드 (신규)** | **Firebase (Cloud Storage + Firestore), Blaze(종량제) 플랜** | 하객 사진 업로드 기능에 필요. 직접 서버(Node/Express 등)를 만들면 별도 호스팅·배포 파이프라인이 또 필요해져 복잡도가 커짐. Firebase는 클라이언트 SDK로 브라우저에서 바로 업로드 가능한 관리형 서비스라 **프론트는 계속 정적 배포(GitHub Pages) 유지, 백엔드는 서버 운영 없이 사용** 가능. 결혼식 당일 트래픽 집중 시 무료 Spark 플랜의 일일 한도로 기능이 막히는 것을 막기 위해 **Blaze 플랜 + 예산 알림**으로 진행 (9장 참고) |
| 배포 (프론트) | **GitHub Actions → GitHub Pages** | 저장소를 GitHub에 올리는 것과 배포를 완전히 동일 생태계로 처리. 별도 서비스(Vercel 등) 가입 불필요, push만 하면 자동 빌드·배포 |
| 패키지 매니저 | npm | 기본값, 특별한 이유 없으면 단순하게 |

> 참고: Vercel도 자동배포엔 훌륭하지만 GitHub 계정 외 별도 서비스 연동이 필요합니다. "github에도 올릴 것"이라는 조건에 가장 깔끔히 맞는 건 GitHub Pages입니다. 나중에 커스텀 도메인이 필요해지면 Vercel로 전환도 어렵지 않습니다.
>
> 참고2: Firebase 대신 Supabase(Postgres 기반)도 대안이 될 수 있습니다. 사진 업로드처럼 단순 파일 저장 + 목록 조회 위주라면 Firebase Storage 쪽이 국내 청첩장 사례 문서·튜토리얼이 많아 진행이 더 수월합니다. 특별한 이유 없으면 Firebase로 진행하겠습니다.

---

## 3. 폴더 구조 (예정)

```
wedding/
├── PLAN.md
├── .github/
│   └── workflows/
│       └── deploy.yml          # main 브랜치 push 시 자동 빌드+배포
├── public/
│   └── images/                 # 갤러리, 메인 사진 등
├── src/
│   ├── main.tsx
│   ├── App.tsx
│   ├── config/
│   │   ├── weddingInfo.ts      # 날짜, 장소, 지도 좌표, 인사말 문구
│   │   └── senderProfiles.ts   # ★ 발신자별 분기 데이터 (핵심)
│   ├── hooks/
│   │   └── useSenderProfile.ts # URL 쿼리 파싱 → 프로필 적용
│   ├── lib/
│   │   └── firebase.ts         # Firebase 초기화 (env 변수로 config 주입)
│   ├── components/
│   │   ├── MainVisual.tsx
│   │   ├── Greeting.tsx
│   │   ├── Calendar.tsx
│   │   ├── LocationMap.tsx
│   │   ├── Gallery.tsx
│   │   ├── AccountInfo.tsx     # 계좌번호 (분기 적용)
│   │   ├── GuestPhotoUpload.tsx  # ★ 하객 사진 업로드 폼 (이름 + 코드 입력 포함)
│   │   ├── GuestPhotoGallery.tsx # ★ 업로드된 사진 갤러리 (실시간 조회)
│   │   └── ShareKakao.tsx
│   └── styles/
├── index.html
├── vite.config.ts
├── tailwind.config.ts
├── package.json
├── .env.local                  # Firebase config (커밋 금지, .gitignore 처리)
└── tsconfig.json
```

---

## 4. 핵심 기능 명세 — 발신자(받는 대상) 분기

### 4.1 개념
청첩장 링크에 쿼리 파라미터를 붙여 카테고리별로 다른 링크를 발송합니다.

```
https://ara-ro.github.io/noels_wedding/?to=parent-bride
https://ara-ro.github.io/noels_wedding/?to=parent-groom
https://ara-ro.github.io/noels_wedding/?to=groom
https://ara-ro.github.io/noels_wedding/?to=bride
https://ara-ro.github.io/noels_wedding/?to=family
```

### 4.2 분기 데이터 모델 (`src/config/senderProfiles.ts`)

| 항목 | 설명 |
|---|---|
| `key` | `groom` / `bride` / `parent-bride` / `parent-groom` / `family` |
| `fontScale` | 기본 대비 배율. 부모님/친척 대상은 가독성 위해 확대(예: 1.15~1.25배), 신랑·신부 본인 대상은 기본(1.0) |
| `nameOrder` | 표시 순서. 예: 신랑측 발송 시 `[신랑, 신부]`, 신부측 발송 시 `[신부, 신랑]` |
| `displayNames` | 문구 자체 변경. 예: 부모님용은 "아들 ○○○ · 며느리 될 ○○○" / 신랑신부 본인용은 "저희 두 사람" |
| `accountOrder` | 계좌 노출 우선순위. 예: 신부측에게는 신부측 계좌 먼저, 혼주(부모)에게는 혼주 계좌 우선 노출 |
| `accountVisibility` | 특정 계좌를 아예 숨길지 여부 (예: 신랑측 지인에게는 신부 부모님 계좌 비노출 등, 필요 시) |

### 4.3 동작 방식
1. `useSenderProfile` 훅이 `window.location.search`에서 `to` 파라미터를 읽음
2. 값이 없거나 매칭 안 되면 **기본 프로필**(신랑신부 공통, 기본 폰트, 양가 계좌 모두 노출)로 폴백 — 링크 오분배 시에도 깨지지 않도록
3. 각 컴포넌트(`Greeting`, `AccountInfo` 등)는 이 프로필 컨텍스트를 구독해 이름 순서/문구/폰트/계좌 순서를 렌더링
4. 잘못된 `to` 값 방어: 화이트리스트 검증 후 미스매치면 기본값 처리 (URL 파라미터를 그대로 신뢰하지 않음)

### 4.4 예시 표

| 받는 대상 (`to`) | 폰트 배율 | 이름 표기 순서 | 이름 문구 예시 | 계좌 우선순위 |
|---|---|---|---|---|
| `groom` | 1.0 | 신랑 → 신부 | "저희 두 사람" | 양가 순서대로 |
| `bride` | 1.0 | 신부 → 신랑 | "저희 두 사람" | 양가 순서대로 |
| `parent-groom` | 1.2 | 신랑 → 신부 | "아들 ○○○ · 며느리 될 ○○○" | 신랑측 계좌 우선 |
| `parent-bride` | 1.2 | 신부 → 신랑 | "딸 ○○○ · 사위 될 ○○○" | 신부측 계좌 우선 |
| `family` | 1.15 | 신랑 → 신부 (또는 협의) | "조카 ○○○ 결혼합니다" 등 | 양가 모두 노출 |

> **결정 (2026-07-25)**: 위 표의 문구·배율은 초안 상태로 두고, **디자인 확정 시 재작성**하기로 함. `senderProfiles.ts`는 이 초안 구조를 그대로 스캐폴딩해두되, 실제 문구 값은 디자인이 나온 뒤 채워 넣는 방식으로 진행 (구조 자체는 지금 만들어도 무방하므로 개발 착수를 막는 항목은 아님).

---

## 5. 핵심 기능 명세 — 하객 사진 업로드

### 5.1 개념
하객이 결혼식 당일(또는 이후) 본인 스마트폰으로 찍은 사진을 청첩장 페이지에서 바로 업로드하면, 다른 하객들도 볼 수 있는 공용 갤러리에 모이는 기능. 별도 앱 설치 없이 모바일 브라우저에서 바로 동작해야 함.

### 5.2 구조
- **업로드**: `GuestPhotoUpload` 컴포넌트 → **업로더 이름 + 코드(비밀번호) 입력** → 파일 선택(또는 카메라 촬영) → 브라우저에서 클라이언트 사이드 리사이즈/압축(예: 긴 변 1920px, JPEG 품질 80%로 축소 — 원본 그대로 올리면 용량·속도·무료 티어 한도 문제) → Firebase Storage 업로드 → 업로드 완료 후 Firestore에 파일 URL·업로더 이름·업로드 시각 메타데이터 기록
- **조회**: `GuestPhotoGallery` 컴포넌트 → Firestore를 실시간 구독(`onSnapshot`) 또는 최초 1회 조회 → 최신순 그리드 표시, 무한 스크롤 또는 페이지네이션

### 5.3 업로드 인증 방식 — 이름 + 코드(1107) 확정
**결정 (2026-07-25)**: 업로드 시 이름과 함께 예식일 기반 4자리 코드(`1107`)를 입력받는 방식으로 진행.

- **구현 방식**: 이 코드는 단순히 화면(JS)에서만 체크하면 개발자도구로 우회해 직접 Firebase에 쓰기 요청을 보낼 수 있으므로, **Firebase 보안 규칙 단에서도 함께 검증**합니다.
  - Storage 업로드 시 커스텀 메타데이터에 코드값을 실어 보내고, Storage 보안 규칙이 `request.resource.metadata.code == '1107'`인지 확인 후에만 쓰기 허용
  - Firestore에 메타데이터(이름·URL 등) 기록 시에도 동일하게 규칙에서 `request.resource.data.code == '1107'` 확인
  - 즉, 코드 검증이 클라이언트 UI뿐 아니라 Firebase 서버(규칙 엔진) 단에서도 최종적으로 강제됨
- **보안 수준에 대한 안내**: 정적 사이트 특성상 이 코드 값은 결국 공개된 JS 번들 안에 존재하므로, 마음만 먹으면 알아낼 수 있는 수준의 **약한 스팸 방지 장치**입니다. 다만 목적이 "초대받지 않은 봇/크롤러의 무작위 업로드 차단"이라 이 정도로 충분하며, 실제 하객은 예식일(11/07)을 이미 알고 있으므로 사용성에는 문제가 없습니다. 더 강한 보안이 필요하시면 별도로 말씀해주세요 (Cloud Functions 기반 서버 검증 등으로 강화 가능하나 구현 복잡도가 늘어남).

### 5.4 보안/운영 고려사항
- **Firebase Storage/Firestore 보안 규칙**: 클라이언트에서 직접 쓰기가 가능한 구조이므로, 규칙으로 반드시 제한 필요
  - 업로드 파일 형식: 이미지(`image/*`)만 허용
  - 파일 크기 상한 (예: 10MB, 실제로는 5.2의 클라이언트 압축으로 대부분 1MB 이하가 됨)
  - 코드(`1107`) 일치 여부 (5.3 참고)
- **부적절한 사진 삭제(운영/모더레이션)**: 별도 관리자 페이지를 만들 수도 있으나, 초기 버전은 **Firebase 콘솔에서 직접 삭제**하는 방식으로 충분(청첩장 특성상 하객 수가 한정적, 업로드 코드 인증으로 무분별한 업로드 자체가 이미 걸러짐). 필요시 추후 간단한 비밀번호 기반 관리자 화면 추가 가능
- **비용**: 결혼식 당일 트래픽 집중 시 무료 Spark 플랜의 일일 한도 초과로 기능이 막히지 않도록 **Blaze(종량제) 플랜 + 예산 알림**으로 운영 (자세한 근거와 설정값은 9장 "트래픽 및 가용성 대책" 참고). 사진 업로드 전 압축은 여전히 비용·용량 관리에 중요

### 5.5 GitHub Actions 연동 시 주의점
Firebase 설정값(`apiKey`, `projectId` 등)은 빌드 시 `.env` 로 주입되는데, 이 값들은 클라이언트에 어차피 노출되는 공개 설정값이라 커밋되어도 보안상 큰 문제는 없지만(Firebase는 이 값 노출을 전제로 설계됨), 실제 보안은 위 5.3의 **Storage/Firestore 규칙**이 담당합니다. 그래도 관례상 `.env.local`은 `.gitignore` 처리하고, **GitHub Actions repository secrets**에 등록해 빌드 시점에 주입합니다.

---

## 6. 페이지 섹션 구성 (일반적인 모바일 청첩장 구조)

1. 메인 비주얼 (사진 + 이름 + 예식일)
2. 인사말 (발신자별 분기 적용)
3. 캘린더 / D-Day
4. 예식 안내 (일시, 장소, 지도, 대중교통·주차 안내)
5. 갤러리 (신랑신부가 준비한 사진 그리드/슬라이더)
6. 마음 전하실 곳 (계좌번호, 발신자별 분기 적용, 계좌 복사 버튼)
7. **하객 사진 업로드 + 갤러리** (신규, 5장 참고)
8. 카카오톡 공유 버튼 / 링크 복사

---

## 7. 방명록·RSVP — 미포함 (결정)

**결정 (2026-07-25)**: 방명록/참석여부(RSVP) 기능은 이번 범위에 **포함하지 않음**. 폴더 구조의 `GuestBook.tsx`, 관련 컴포넌트는 스캐폴딩 단계에서 만들지 않습니다. (향후 필요해지면 하객 사진 업로드와 동일한 Firebase 백엔드를 재사용해 추가 가능합니다.)

---

## 8. 배포 파이프라인

- **프론트엔드**: `main` 브랜치 push → GitHub Actions 트리거 → `npm ci && npm run build` (빌드 시 Firebase config를 Actions secrets에서 주입) → `dist/` 를 GitHub Pages로 배포
- **백엔드(Firebase)**: 별도 배포 파이프라인 불필요 — Firebase 콘솔에서 프로젝트 생성 및 Storage/Firestore 보안 규칙만 설정하면 바로 사용 가능 (규칙 변경은 Firebase CLI로 자동화도 가능하나, 초기엔 콘솔에서 수동 설정으로 충분)
- `vite.config.ts`에 `base: '/noels_wedding/'` (리포지토리명과 일치) 설정 필요 — 프로젝트 페이지 서브패스 대응. 최종 URL: `https://ara-ro.github.io/noels_wedding/`
- 워크플로우 파일 `.github/workflows/deploy.yml` 초안은 개발 시작 시 함께 작성

---

## 9. 트래픽 및 가용성 대책 (결혼식 당일 다운 방지)

### 9.1 프론트엔드 (GitHub Pages)
CDN(Fastly) 기반 정적 호스팅이라 결혼식 당일 트래픽이 몰려도 인사말·계좌번호·지도 등 본문 페이지가 "다운"될 가능성은 사실상 없습니다. 별도 조치 불필요.

### 9.2 백엔드 (Firebase) — 확정 사항
- **Blaze(종량제) 플랜 사용**: 무료 Spark 플랜의 일일 한도 초과로 사진 업로드 기능이 당일 멈추는 상황을 방지. Firebase 콘솔에서 카드 등록 후 전환.
- **예산 알림(Budget Alert) 설정**: 예상치 못한 과금(예: 악의적 대량 업로드) 방지용 안전장치. 하객 100~300명 규모에서 실제 예상 비용은 매우 낮은 수준(수천 원 이내)이며, 여유 있는 임계값(예: 10,000원/20,000원 구간)으로 알림을 걸어 이상 징후를 조기 감지.
- **트래픽 설계 기준**: 하객 100~300명. Blaze 플랜에서는 Firestore/Storage가 자동 스케일링되므로 이 규모에서 별도 서버 증설이나 사전 용량 확보가 필요 없음.
- **업로드 총량 제한**: 1인당 업로드 개수는 제한하지 않되, **전체 업로드 총 용량 상한**을 두어 도달 시 "업로드가 마감되었습니다" 안내로 전환(보안 규칙 + 클라이언트 체크 이중 처리).
- **클라이언트 사이드 이미지 압축**(5.2 참고)이 이 총량 제한을 실효성 있게 만드는 핵심 장치 — 압축 없이는 소수 인원의 원본 업로드만으로도 용량이 급증할 수 있음.

### 9.3 용량/비용 산정 및 상한 제안

**전제 값**
- 클라이언트 압축 기준: 긴 변 1920px, JPEG 품질 80% → 사진 1장당 평균 **0.3~0.5MB** (스마트폰 원본은 보통 3~8MB지만 리사이즈 후 대폭 감소)
- 하객 규모: 100~300명 (확정된 값)
- 실제 업로드 참여율: 청첩장에 사진 업로드 기능을 넣어도 전원이 올리진 않음. 통상 참석 하객의 **20~40%** 정도가 사진을 올리고, 1인당 평균 **3~5장** 정도 업로드하는 것이 일반적인 모바일 청첩장/웨딩스냅 공유 사례의 체감치

**추정 계산**
| 시나리오 | 업로더 수 | 1인당 장수 | 총 사진 수 | 예상 용량(압축 후) |
|---|---|---|---|---|
| 보수적(적게 참여) | 60명 (300명의 20%) | 3장 | 180장 | 약 60~90MB |
| 평균적 | 120명 (300명의 40%) | 5장 | 600장 | 약 200~300MB |
| 넉넉하게 잡은 최대치 | 300명 전원 | 10장 | 3,000장 | 약 1~1.5GB |

즉 실사용 시나리오는 **수백 MB** 수준이 유력하고, "전원이 넉넉하게 업로드"하는 극단적 상황을 가정해도 **1.5GB 안팎**이면 충분히 커버됩니다.

**비용 환산 (Firebase Blaze 요금 기준, 대략치)**
- Storage 저장 비용: GB당 월 약 $0.026 (약 35원/월) — 4GB를 한 달 유지해도 약 140원 수준
- Storage 다운로드(조회) 비용: GB당 약 $0.12 (약 160원) — 하객들이 갤러리를 여러 번 열어보며 반복 조회하는 것이 실제로는 저장 비용보다 더 큰 비중을 차지함
  - 예: 총 4GB 사진을 하객들이 합쳐서 5회씩 다시 보면(썸네일 없이 원본을 계속 로드한다는 보수적 가정) 약 20GB 다운로드 ≈ 3,200원 내외
- **결론**: 4GB 상한 기준으로도 실제 예상 비용은 **한 달 합계 3,000~5,000원 내외**로, 9.2에서 설정한 예산 알림(10,000원/20,000원) 안에 여유 있게 들어옵니다.

**추가 권장(선택)**: 갤러리 그리드에서는 축소 썸네일을, 사진을 탭했을 때만 원본을 불러오도록 하면(Firebase Extensions "Resize Images" 등 활용) 반복 조회로 인한 다운로드 비용을 크게 줄일 수 있습니다. 트래픽이 몰리는 결혼식 당일 안정성에도 도움이 되므로 권장드립니다.

**결정 (2026-07-25)**: 총 업로드 용량 상한 **4GB**로 확정 (평균 시나리오 대비 약 13배, 최대 시나리오 대비도 넉넉한 버퍼).

### 9.4 부하 테스트
결혼식 전, 여러 기기로 동시 업로드를 시뮬레이션하는 부하 테스트를 1회 진행해 병목 여부를 사전에 확인하는 것을 권장합니다.

---

## 10. 진행해야 할 작업 목록 (체크리스트)

### 10.1 확정된 사항
- [x] GitHub 저장소: `https://github.com/ara-ro/noels_wedding` → Pages URL `https://ara-ro.github.io/noels_wedding/`
- [x] 발신자 5종 문구: 구조만 먼저 스캐폴딩, 실제 문구는 디자인 확정 후 재작성 (4.4 참고)
- [x] 방명록/RSVP: 미포함 (7장 참고)
- [x] 하객 사진 업로드: 이름 + 코드(`1107`) 입력 방식, Firebase 보안 규칙으로 서버 단 검증 (5.3 참고)
- [x] Firebase 요금제: Blaze(종량제) + 예산 알림(기본안 10,000원/20,000원)
- [x] 업로드 총 용량 상한: **4GB** (9.3 참고)
- [x] 부적절한 사진 삭제: Firebase 콘솔 수동 처리

### 10.2 사용자가 준비해주셔야 하는 것
- [ ] 신랑/신부 이름 (한글/영문), 예식 일시, 장소명·주소
- [ ] 사용할 사진 (메인 1~2장, 갤러리용 다수)
- [ ] 인사말 문구 (또는 초안 작성 요청 여부)
- [ ] 계좌번호 목록 (신랑/신부/혼주 각각, 은행명·예금주)
- [ ] 지도 표시용 API 키 (카카오맵 또는 네이버지도 - 무료 발급)
- [ ] 카카오톡 공유 기능용 Kakao JavaScript 키 (Kakao Developers 무료 발급)
- [ ] **Google 계정으로 Firebase 프로젝트 생성 + Blaze 플랜 전환(카드 등록)** (console.firebase.google.com)

### 10.3 개발 진행 순서 (마일스톤)
1. 프로젝트 스캐폴딩 (Vite+React+TS+Tailwind) 및 GitHub 저장소/Actions 배포 파이프라인부터 먼저 구축 → 빈 페이지라도 "배포가 되는 상태"를 최우선 확보
2. 정적 레이아웃(섹션 8개) 퍼블리싱 — 목업 데이터로 UI 완성
3. 발신자 분기 로직(`senderProfiles`, `useSenderProfile`) 적용
4. Firebase 프로젝트 연결 (Blaze 전환, 예산 알림, Storage/Firestore 초기화 및 보안 규칙·업로드 총량 제한 작성) + 하객 사진 업로드·갤러리 구현
5. 지도/카카오공유 등 외부 SDK 연동
6. 실제 컨텐츠(사진/문구/계좌) 반영
7. 모바일 실기기 테스트 (iOS Safari, Android Chrome, 카카오톡 인앱 브라우저 — 특히 인앱 브라우저 호환성 및 파일 업로드 동작 확인 중요)
8. 동시 업로드 부하 테스트 (9.4 참고)
9. 최종 배포 및 링크별(`?to=`) QA

---

## 11. 다음 액션

모든 핵심 의사결정이 완료되었습니다 (10.1 참고). 10.2의 콘텐츠(이름/날짜/장소/사진/계좌/지도·카카오 키/Firebase 프로젝트)는 개발 진행과 병행해서 순차로 전달해주셔도 되며, 바로 스캐폴딩을 시작할 수 있습니다.
